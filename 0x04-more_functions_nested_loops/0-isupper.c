#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks whether a character is ppercase
 * @c: character to be checked
 * Return: 1 - true; 0 - false
 */

int _isupper(int c)
{
	return (c > 96 && c < 123);
}
