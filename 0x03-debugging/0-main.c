#include "main.h"

/**
 * main - tests the function that prints whether an int is negative or positive
 * Return: (0) success
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
